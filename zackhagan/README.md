# Zack Hagan React site

A refreshed React + Vite single-page site that spotlights Zack Hagan's music and development work.

## Getting started

```bash
npm install
npm run dev
```

Open the dev server output (default `http://localhost:5173`) to view the site. Build for production with `npm run build` and preview the static output using `npm run preview`.
